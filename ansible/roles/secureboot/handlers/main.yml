---
- name: Deploy signed bootloader
  become: true
  ansible.builtin.command:
    cmd: bootctl install
  changed_when: true

- name: Restart fwupd
  become: true
  ansible.builtin.systemd_service:
    name: fwupd.service
    state: restarted
